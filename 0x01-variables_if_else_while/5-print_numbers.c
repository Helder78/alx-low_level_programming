#include <stdio.h>

/**
 * main - Prints all single digit number of base 10 starting from 0
 *
 * Return: Always (success)
 */
int main(void)
{
  a=0;
  do
    {
      printf("%d\n",a);
      a++;
    }while (a <= 10);
  
  return (0);
}
